<%= form_tag(home_path, :method => "get", id: "search") do %>
  <%= text_field_tag :search, params[:search], class: "form-control", id: "search-input", placeholder: "Search Products" %>
  <button class="btn btn-primary" type="submit">Search</button>
<% end %>
